apiVersion: v1
kind: Service
metadata:
  name: olivetti-app-service
spec:
  type: NodePort
  selector:
    app: olivetti-app
  ports:
    - port: 5000        # service port
      targetPort: 5000  # container port
      nodePort: 30000   # external port on the node (30000–32767)
